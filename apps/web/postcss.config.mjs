// Configuration PostCSS minimaliste pour Tailwind v4.
// Tailwind v4 utilise un plugin PostCSS dédié `@tailwindcss/postcss`.

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
