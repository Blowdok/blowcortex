# @blowcortex/ui

Composants partagés (shadcn/ui) et primitives React.

**Statut :** squelette (Sprint 1) — exporte `cn()` (utilitaire Tailwind). Les composants shadcn seront ajoutés via `npx shadcn@latest add ...` directement dans `apps/web` au Sprint 1, puis remontés ici dès qu'ils sont partagés.
